## What

<!-- One sentence describing the change. -->

## Why

<!-- The motivation. Link the issue / audit entry if there is one. -->

## Notes

- [ ] Touches KDP cover math? Golden tests still pass (`pnpm test`).
- [ ] Touches `lib/site-config.ts`? Updated tokens in `app/globals.css` too.
- [ ] Touches `public/network.json` or `public/ai-bots.json`? Synced from `vertexnetwork/hub`, not edited locally.
- [ ] Touches the legal pages? Provider list still matches `siteConfig.features.*`.
