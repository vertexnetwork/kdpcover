# Launch kit — Cover Pass-Check on Gumroad

Everything to launch the paid product. Copy-paste into Gumroad, set the Vercel env
vars, flip two flags. Imagery is generated by `npm run build:images` →
`temp/gumroad-product/`. Bonus zip by `npm run build:templates`.

---

## 1. Product setup (Gumroad dashboard)

- **Name:** `Cover Pass-Check — pass KDP's review on the first try`
- **Permalink:** `cover-pass-check` → `https://kdpcover.gumroad.com/l/cover-pass-check`
- **Type:** Digital product · **License keys: ON** (Settings → "Generate a unique license key per sale")
- **Price / versions:** create **two versions** (Gumroad "Versions"):
  | Version name | Price | Notes |
  |---|---|---|
  | **Author** | **$19** | default selection |
  | **Studio** | **$49** | name must contain one of `GUMROAD_STUDIO_VARIANTS` (default: studio, volume, batch) |
  > The site reads the version name off the license to unlock batch mode. Keep "Studio" in the name or update `GUMROAD_STUDIO_VARIANTS` to match.
- **Content / file:** upload `cover-pass-check-bonus.zip` (the 2,500-template bonus pack — both versions get it).
- **Refund policy:** 7 days (Gumroad Settings → matches the copy below).
- **Cover image:** `cover.png`. **Thumbnail:** `thumbnail.png`.
- **Preview images (in this order):** `preview-1.png` (the report), `preview-2.png` (wrong-size rejection), `preview-3.png` (what's inside), `preview-4.png` (Author vs Studio), `preview-5.png` (how it works).

---

## 2. Listing description (paste into Gumroad)

> **Will KDP reject your cover? Find out in 10 seconds — before you upload.**
>
> You designed the cover. You're about to hit publish. And somewhere in the back of your mind: *what if KDP bounces it and I lose three days re-doing the file?*
>
> That fear is justified. KDP rejects covers every day for the same handful of reasons — and your design tool won't warn you about any of them:
>
> - The file is **trim-sized** — the 0.125″ bleed is missing on every edge (a 0.25″ gap = automatic rejection).
> - The **full-cover size** is off for your exact trim, paper, and page count.
> - A **font isn't embedded**, so the spine text reflows on KDP's press.
> - It's **RGB** instead of CMYK, so the colors shift in print.
> - The resolution is under **300 DPI** at print size.
>
> **Cover Pass-Check reads your finished cover and checks every one of these against KDP's spec *and your specific book* — then tells you exactly what to fix.** Upload your PDF (or PNG/JPG), set your trim, paper, and page count, and get an instant pass/fail report. No guesswork, no failed upload, no multi-day delay.
>
> **It runs entirely in your browser.** Your cover file is never uploaded, never stored, never seen by anyone. Same privacy-first approach as the free calculator at kdpcover.pro.
>
> ### What you get
> - **The in-browser file checker** — every KDP cover rule, auto-verified: full-cover size, bleed/wrap, resolution, embedded fonts, color space, single-page, file size.
> - **The exact fix for every problem** — not just "rejected," but *what to change*.
> - **Bonus: the 2,500-template pack** — ready-to-design SVG + PDF templates for every common KDP trim, paper, and page-count step, with the spine, safe zones, and barcode area pre-positioned.
> - **A one-page cheat sheet** of every multiplier, bleed, hinge, and barcode rule.
> - **Lifetime access + free updates** — if KDP changes a spec, you get the new version.
>
> ### Two versions
> - **Author — $19.** For your book launch. Check one cover at a time, as many times as you need.
> - **Studio — $49.** For volume publishers (planners, journals, low/no-content). Batch mode: drop a whole folder of covers and get a worst-first pass/fail table with CSV export.
>
> ### How to use it
> 1. Buy → you get a license key on your receipt.
> 2. Go to **kdpcover.pro/pass-check**, paste the key once to unlock the tool on your device.
> 3. Set your book, drop your cover, read the verdict. Fix anything flagged and re-check.
>
> **7-day, no-questions-asked refund.** If it doesn't catch something worth fixing, email your receipt and we'll refund you.
>
> *Independent tool — not affiliated with Amazon, Kindle Direct Publishing, or any KDP trademark.*

---

## 3. Receipt / post-purchase content (Gumroad "Content" + receipt note)

Paste this as the product **Content** (shown on the download/receipt page and emailed):

> ## Thanks — let's make sure your cover passes. 🎉
>
> **Your license key is on this receipt and in your confirmation email.**
>
> **To unlock Cover Pass-Check:**
> 1. Go to **https://kdpcover.pro/pass-check**
> 2. Paste your **license key** when prompted.
> 3. That's it — the tool unlocks on this device (no account, no password). One purchase covers your own laptop and phone.
>
> **Your bonus 2,500-template pack** is the file attached to this purchase — download, unzip, and open any template in Affinity, Illustrator, Inkscape, Figma, or Canva.
>
> **How to run a check:** set your format, trim, paper, and page count → drop your finished cover (PDF/PNG/JPG) → read the pass/fail report. Everything runs in your browser; your file is never uploaded.
>
> **Studio buyers:** your key unlocks batch mode automatically — drop a whole folder of covers at /pass-check.
>
> Questions or a key that won't activate? Email **hello@kdpcover.pro** and we'll sort it fast. 7-day refund, no questions asked.
>
> — The kdpcover.pro team

Short **receipt note** (if a separate field): *"Your license key is above. Unlock the tool at kdpcover.pro/pass-check — paste the key once, no account needed. Bonus template pack is attached. 7-day refund."*

---

## 4. Vercel environment variables

Set in **Vercel → Project → Settings → Environment Variables** (Production), then redeploy.

**Server-only (encrypted, no NEXT_PUBLIC_):**
| Key | Value |
|---|---|
| `GUMROAD_PRODUCT_ID` | the product id from Gumroad (Product → Advanced → "product_id"; post-2023 products verify by id, not permalink) |
| `PREFLIGHT_TOKEN_SECRET` | a long random string (e.g. `openssl rand -hex 32`) — the HMAC key for the unlock cookie |
| `PREFLIGHT_USES_CAP` | `5` (activations before sharing is blocked) |
| `GUMROAD_STUDIO_VARIANTS` | `studio,volume,batch` (only if your Studio version is named differently) |

**Public:**
| Key | Value |
|---|---|
| `NEXT_PUBLIC_GUMROAD_PRODUCT_URL` | `https://kdpcover.gumroad.com/l/cover-pass-check` |
| `NEXT_PUBLIC_GUMROAD_STUDIO_URL` | `https://kdpcover.gumroad.com/l/cover-pass-check?option=<studio_variant_id>` |
| `NEXT_PUBLIC_GUMROAD_ENABLED` | `1` |
| `NEXT_PUBLIC_GUMROAD_PRICE` | `19` |
| `NEXT_PUBLIC_GUMROAD_STUDIO_PRICE` | `49` |
| `NEXT_PUBLIC_PREFLIGHT_ENABLED` | `1` |

---

## 5. Go-live checklist

1. `npm run build:templates` → `Compress-Archive -Path dist/templates/* -DestinationPath dist/cover-pass-check-bonus.zip`
2. `npm run build:images` → upload `temp/gumroad-product/*` to Gumroad (cover, thumbnail, 5 previews in order).
3. Create the Gumroad product (§1), paste the description (§2) and content/receipt (§3), enable license keys, add Author + Studio versions.
4. Set the Vercel env vars (§4) and redeploy.
5. **Smoke test:** buy the Author version (or use a test license), go to `/pass-check`, paste the key → tool unlocks. Drop a known-bad cover (trim-sized) → confirm it fails on size + bleed. Buy/borrow a Studio key → confirm batch mode appears.
6. Confirm the public landing `/cover-pass-check` shows live "Get Pass-Check $19" buttons (not "Notify me").

**Before launch (flags off):** the public landing renders with "Notify me" CTAs and `/pass-check` 404s — safe to merge and let SEO compound until you flip the flags above.
