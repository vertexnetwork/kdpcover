import "../globals.css";

export const metadata = {
  title: "KDP Cover Calculator — Embed",
};

export default function EmbedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
